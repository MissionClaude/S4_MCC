"""Tests for Phase 1 models.

Each test file targets one module of the ``models`` package:

* ``test_tools`` — ToolDefinition, ToolCall, ToolResult, AgentConfig
* ``test_messages`` — Message types, StopReason
* ``test_api`` — APIRequest, APIResponse, structured errors
"""
